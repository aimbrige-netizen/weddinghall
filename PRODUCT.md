# PRODUCT.md — Sding 웨딩홀 견적 검산기

> impeccable `init` 산출물. 제품의 변하지 않는 진실만 기록합니다.
> 시각 결정은 [DESIGN.md](DESIGN.md), 기능 스펙은 [견적서.md](견적서.md).

## 한 줄 정의

웨딩홀에서 받아온 견적서의 숫자를 **청구인원 기준으로 다시 계산해**, 총 예상비용 · 1인당 실질단가 · 허수인원을 돌려주는 모바일 도구.

## 고유 메커니즘

식대 단가는 **참석인원이 아니라 청구인원(보증인원과 참석인원 중 큰 값)** 에 곱해진다.
이 한 줄이 견적서를 읽는 방식 전체를 바꾼다. 1인당 실질단가는 총비용을 **청구인원**으로 나눠 구한다.

## 사용 장면

- **누가**: 예식 6~12개월 전, 웨딩홀 투어를 도는 20대 후반~30대 중반 예비부부.
- **어디서**: 홀 투어 직후 차 안, 또는 밤에 집에서. **한 손엔 휴대폰, 한 손엔 종이/PDF 견적서.**
- **무엇을**: 견적서의 숫자 20개 남짓을 옮겨 적는다. 한 번에 끝내지 못하고 중간에 끊긴다.
- **왜**: 홀 A와 홀 B 중 어디가 실제로 싼지 판단이 안 서서.

→ 이 장면이 강제하는 것: **밝은 화면**(주간 차 안), **엄지 도달 범위의 하단 액션**, **새로고침해도 살아남는 입력값**, **숫자 키패드에 최적화된 입력**.

## 성공의 정의

1. 입력을 끝까지 완주한다 (이탈 없이).
2. 결과 화면에서 **1인당 실질단가**를 보고 견적서의 체감 가격이 달라진다.
3. 홀 2~3곳을 비교함에 담는다.

## 모드

**Operate** (사용자가 과업 중) + 첫 뷰포트만 **Persuade**.
표현이 과업·상태·익숙한 어포던스를 가려서는 안 된다.

## 브랜드 약속 (변경 불가)

- 브랜드명 **Sding**, 로고는 제공된 SVG 원본 비율 유지. 재색상 금지.
- 브랜드 오렌지 `#FF5416`.
- 정식 명칭은 **견적 검산기** (검사기 아님).
- 면책 문구 상시 노출: 최종 금액은 계약서 원문으로 확인.

## 제약

- **백엔드 없음.** 계산·비교·저장 전부 클라이언트. (기획서 2-7)
- **빌드 스텝 없음.** 개발 환경에 Node/npm이 없다. 정적 파일만으로 동작해야 한다.
- 배포 대상: GitHub Pages (`aimbrige-netizen/weddinghall`).

## 손대면 안 되는 것

- 기획서 1-3의 계산 산식. 이 도구의 신뢰가 여기 걸려 있다.
- 금액 칸의 예시값 프리필 + "예시입니다" 안내. 빈 폼은 이탈을 만든다.
- **★ 예상 참석인원(`groomExpected` / `brideExpected` / `childCount`)에는 절대 기본값을 넣지 않는다.**
  앱이 채워두면 사용자가 자기가 넣지도 않은 인원으로 계산된 결과를 보게 되고, 그건 곧 앱이
  사용자 대신 판단한 것이다. 이 세 값은 오직 사용자만 정한다. (2026-08-13 사용자 결정)
- **앱이 사용자의 선택을 조용히 바꾸지 않는다.** 값이 없으면 대신 계산하지 말고 입력을 요청한다.

## 미결정 (사용자 확인 필요)

- 축의금 총액 기준을 실제참석(현재) 대신 청구인원으로 볼지 여부. 기획서 1-5에서 미확정으로 남은 항목.

## 결정된 것

- **리드 CTA는 넣지 않는다** (기획서 2-1 / 2-5). 이 도구는 **순수 유틸리티**로 운영한다.
  결과 화면은 계산 결과와 그 근거만 보여주고, 상담 유도로 넘어가지 않는다.
- **입력은 위저드가 아니라 한 페이지 연속 폼** (2026-08-13, 사용자 결정 — 기획서 2-2의 위저드 제안을 뒤집음).
  모바일 히어로는 압축 밴드로 유지하고 첫 화면에서 바로 입력이 보이게 한다.
  위저드 구현은 `v1-wizard` 브랜치에 보존.
- **허수인원은 어떤 화면에도 노출하지 않는다** (2026-08-13, 사용자 결정 — 기획서 1-3의 차별화 포인트를 뒤집음).
  "안 왔는데 결제하는 자리" 류의 단정·훈수 카피, 좌석도, 러스트 경고 블록 전부 제거.
  `calc.js`의 phantom 필드는 엔진 내부 값으로만 남긴다. 재도입은 사용자 지시가 있을 때만.
- **1인당 실질단가의 분모는 사용자가 고른다** (2026-08-13). 기본값 **청구인원**, 선택지 **예상 참석**.
  기획서 1-3은 예상 참석 고정이었으나 뒤집었다. 두 값은 입력 1번 섹션 요약에 항상 나란히 뜨고,
  결과 화면도 고르지 않은 쪽 값을 함께 보여준다.
  **고른 기준을 앱이 바꾸지 않는다.** 나눌 인원이 비면 값을 `—`로 두고 입력을 요청한다.
- **이어받은 값은 이어받았다고 표시한다** (2026-08-13). "새 홀 입력하기"로 넘어온 칸은 점선 테두리 +
  "이전 홀" 라벨 + 상단 안내("N개 칸을 이전 홀에서 가져왔습니다")로 구분하고, 사용자가 손대면 표시가 풀린다.
  '가져온 값 비우기'로 한 번에 지울 수 있고 되돌리기도 붙는다.
- **저장은 계정 없이 localStorage + 링크 백업**. 서버를 두지 않는다.
  견적 금액을 서버에 받지 않으므로 개인정보 보관 책임이 생기지 않고, 사용자가 늘어도 비용이 0이다.
  대신 사파리 7일 삭제·기기 이동·도메인 변경으로 유실되므로 **비교함 링크 복사**가 그 우회로다.
  ★ 도메인을 바꾸면 그 시점까지 저장된 사용자 데이터가 전부 리셋된다. 커스텀 도메인은 이용자가 붙기 전에 확정할 것.
