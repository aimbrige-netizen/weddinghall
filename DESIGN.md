# DESIGN.md — Sding 웨딩홀 견적 검산기

> 빌드된 결과물에서 뽑아낸 시각 시스템. 의도가 아니라 **실제 코드 기준**입니다.
> 제품 진실은 [PRODUCT.md](PRODUCT.md).

## 비주얼 월드 — "재발행된 견적서"

사용자는 **손에 견적서를 들고** 이 화면을 봅니다. 그래서 이 도구는 계산기처럼 생기지 않고,
그 견적서를 **다시 발행한 문서**처럼 생겼습니다.

- 구획은 **카드가 아니라 1px 괘선**이 만든다. 카드를 쌓지 않고, 카드 안에 카드를 넣지 않는다.
- 숫자는 **표 정렬(tabular-nums)** 이 기본. 금액이 세로로 정렬되지 않으면 문서가 아니다.
- 결과 화면은 **발행 헤더**(발행처 · 대상 · 기준)로 시작한다.
- 색은 세 곳에만 쓴다: 진행 상태, 주 액션, 그리고 **허수 구간**.

거부한 것: 파스텔 웨딩 톤(청첩장 클리셰), 핀테크 계산기 톤(파랑 + 둥근 카드 나열),
견출지형 통계 배지, 히어로 위 eyebrow 라벨, 장식용 그라디언트·글래스.

## 컬러

색 전략은 **Restrained + 한 구간 Drenched**. 중립색이 화면을 지배하고, 오렌지는 액션에만,
허수 구간 하나만 완전히 색에 잠긴다.

| 토큰 | 값 | 쓰임 | 대비 |
|---|---|---|---|
| `--paper` | `#F7F6F3` | 캔버스 (따뜻한 본 화이트) | — |
| `--sheet` | `#FFFFFF` | 입력창 · 패널 표면 | — |
| `--ink` | `#0B0B0C` | 문서 헤더 · 하단바 · 본문 최상위 | — |
| `--ink-2` | `#3B3C40` | 본문 | 11.3:1 on sheet |
| `--ink-3` | `#6B6C73` | 보조 텍스트 **하한** | 5.2:1 on sheet |
| `--ink-4` | `#9A9BA2` | **비텍스트 전용** — 괘선 마크, 비활성 | 텍스트 금지 |
| `--rule` / `--rule-2` | `#E3E1DC` / `#D4D2CC` | 괘선 · 입력창 테두리 | — |
| `--orange` | `#FF5416` | 브랜드. 주 버튼, 현재 단계, 진행바 | 3.2:1 on sheet → **큰 글씨·UI 전용** |
| `--orange-ink` | `#C63B00` | 오렌지 계열 **작은 글씨** | 5.1:1 on sheet |
| `--rust` | `#B83200` | 허수 구간 배경 (drenched) | 흰 글씨 6.0:1 |
| `--rust-tint` | `#FFD9C9` | rust 위 보조 텍스트 | 4.6:1 on rust |
| `--onink-2` / `--onink-3` | `#A8A9B0` / `#7B7C83` | 먹색 위 보조 텍스트 | 8.4:1 / 4.7:1 |

`--orange` 를 14px 이하 텍스트에 쓰지 말 것. 그 자리는 `--orange-ink` 다.

## 타이포그래피

**Pretendard Variable** 한 벌로 헤딩·본문·라벨·숫자를 전부 처리한다 (Operate 화면은 display/body 페어링이 필요 없다).
CDN(jsDelivr, dynamic subset) → 시스템 폰트 폴백.

| 역할 | 크기 | 굵기 | 자간 |
|---|---|---|---|
| 히어로 | 30 / 36 / 42px | 780 | -.038em |
| 화면 제목 | 26 / 30px | 780 | -.042em |
| 총액 (결과) | 40 / 48 / 52px | 800 | -.045em |
| 허수 수치 | 30 / 34px | 800 | -.045em |
| 지표 값 | 19–22px | 780 | -.04em |
| 본문 · 설명 | 13.5px | 400 | -.017em |
| 필드 라벨 | 13px | 600 | — |
| 입력값 | **17px** | 650 | -.025em |
| 마이크로 라벨 | 10.5–11px | 650–700 | **+.06 ~ +.11em** |

- 입력창 폰트는 **17px 하한**. 16px 미만이면 iOS Safari가 포커스 시 화면을 확대한다.
- 자간 하한 -.045em. 마이크로 라벨만 양의 자간으로 원장 라벨의 결을 만든다.
- `word-break: keep-all` + `overflow-wrap: break-word` — 한국어를 어절 단위로 끊는다.

## 공간 · 형태

- 8px 리듬. 섹션 26–34px, 필드 간격 14–16px, 거터 20 / 32 / 40px.
- 반경: 입력·버튼 `10px`, 패널 `14px`, 작은 컨트롤만 pill.
- **입면(elevation)은 한 번만 선언한다.** 종이 위 패널은 괘선만, 떠 있는 요소(하단바)는 그림자만.
  1px 테두리 + 넓은 그림자 조합(ghost card)은 쓰지 않는다.
- 터치 타겟 하한 48px (`--tap`).

## 레이아웃

- **모바일**: 단일 컬럼. 먹색 헤더 sticky → **압축 히어로(1줄 헤드라인 + 1줄 부제, ~76px)** →
  5개 섹션이 괘선으로 이어지는 **한 페이지 연속 입력** → 하단 고정바(2단: 숫자행 + 결과 보기).
  입력이 주인공이므로 첫 화면에서 바로 첫 필드가 보여야 한다.
- **≥1000px**: Editorial split. 왼쪽 먹색 레일(로고 · 헤드라인 · 실시간 장부)이 `100vh` sticky,
  오른쪽이 스크롤되는 문서. 하단바는 오른쪽 컬럼 안에서 sticky 캡슐이 된다.
- 결과 · 비교함에서는 폰에서 히어로를 감춘다(헤더 바로 아래에서 결과 시작).
- 비교함은 폰에서 가로 스와이프, 데스크탑에서 3열 그리드.
- 이전 5스텝 위저드 판은 `v1-wizard` 브랜치에 보존.

## 모션

Operate 화면이므로 **상태를 설명하는 모션만**. 라우틴 전환 140–220ms, 진입 연출 560–700ms.
이징은 `cubic-bezier(.16, 1, .3, 1)` 하나로 통일.

- **연출은 딱 하나**: 결과 총액 카운트업(700ms) + 허수 좌석이 채워지는 스태거(총 460ms 상한).
- 나머지는 전부 피드백: 포커스 링, 세그먼트 전환, 버튼 `scale(.975)`, 진행바 폭.
- 스크롤 등장은 `IntersectionObserver` 1회. `prefers-reduced-motion` 에서는 전부 즉시 표시.
- `transform` / `opacity` 만 애니메이트. `backdrop-filter` 는 고정 하단바에만.

## 아이콘

`<symbol>` 스프라이트 하나. viewBox 24, **stroke-width 1.5 고정**, round cap/join, 18px 표시.
이모지를 아이콘으로 쓰지 않는다. 좌석 도트와 disclosure의 `+/−` 는 CSS 도형으로 직접 그린다.

## 브라우저 표면

기본값으로 남겨두지 않는다 — `::selection`(오렌지), `caret-color`, `:focus-visible`(2px 오렌지, 먹색 위에서는 흰색),
`scrollbar-color`, 표 정렬 숫자, `@media print`(결과지를 그대로 출력, 허수 블록은 잉크 절약형 아웃라인으로 전환).

## 콘텐츠 규칙

- 컨트롤은 자기가 하는 일을 이름으로 말한다: "결과 보기", "비교함에 담기", "다른 홀도 계산해서 비교하기".
- 숫자 옆에는 언제나 근거가 붙는다: `250명 × 86,900원`, `실제 참석 205명 기준`.
- 결과 하단 "이 계산이 세운 가정 보기"에 산식 가정을 전부 공개한다. 검산기가 자기 계산을 숨기면 안 된다.
